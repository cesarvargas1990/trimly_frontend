services:
  trimly:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://cesarvargas.tech/trimly/api/v1}
    container_name: trimly-frontend
    ports:
      - "84:80"
    restart: unless-stopped
